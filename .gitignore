# Segredos — nunca devem entrar no histórico do Git
.env
.env.*
*.pem
*.key

# Dependências — pasta gigante, reconstruída automaticamente, não precisa ir pro Git
node_modules/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
